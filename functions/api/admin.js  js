export default {
  async fetch(request) {
    const { searchParams } = new URL(request.url);
    const key = searchParams.get('key');
    if (key !== SECRET_KEY) return new Response('Unauthorized', { status: 401 });
    return new Response(JSON.stringify({ uptime: Date.now() }), { status: 200 });
  }
};
